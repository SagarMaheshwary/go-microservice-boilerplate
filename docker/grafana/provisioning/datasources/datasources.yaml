apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-ds  # Custom UID
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: GET
